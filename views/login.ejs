<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;700&display=swap" rel="stylesheet">
    <style>
        .font-opensans {
            font-family: 'Open Sans', sans-serif;
        }
    </style>
</head>
<body class="flex items-center justify-center min-h-screen bg-blue-600 px-4">
    
    <div class="flex flex-col md:flex-row bg-white p-6 md:p-8 rounded-xl shadow-lg w-full max-w-lg md:max-w-2xl transform transition-all duration-300 hover:scale-105">
        <div class="w-full md:w-1/2 flex items-center justify-center mb-4 md:mb-0">
            <img src="/images/asd.png" alt="Login Image" class="w-32 md:w-full h-auto rounded-lg">
        </div>
        <div class="w-full md:w-1/2 md:pl-6 flex flex-col justify-center">
            <h2 class="text-2xl font-bold text-center text-gray-800 mb-4 font-opensans">wordbridge</h2>
            <% if (error) { %>
            <p class="text-red-600 text-center font-semibold mb-4 bg-red-100 p-2 rounded">
                <%= error %>
            </p>
            <% } %>

            <form action="/login" method="POST">
                <div class="mb-3">
                    <label class="block text-gray-700 font-medium">Email:</label>
                    <input type="email" name="email" class="w-full p-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-400 focus:outline-none" required />
                </div>
                <div class="mb-3">
                    <label class="block text-gray-700 font-medium">Password:</label>
                    <input type="password" name="password" class="w-full p-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-400 focus:outline-none" required />
                </div>
                <button type="submit" class="w-full bg-blue-600 text-white p-2 rounded-lg font-semibold hover:bg-blue-700 transition duration-300">
                    Login
                </button>
            </form>
        </div>
    </div>
</body>
</html>
